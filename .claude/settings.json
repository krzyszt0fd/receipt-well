{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json,subprocess; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); sys.exit(subprocess.call(['dotnet','build','src/backend/ReceiptWell.sln','--no-restore']) if f.endswith('.cs') else 0)\"",
            "timeout": 30,
            "statusMessage": "Checking compilation..."
          },
          {
            "type": "command",
            "command": "python -c \"import sys,json,subprocess; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); sys.exit(subprocess.call(['dotnet','restore','src/backend/ReceiptWell.sln']) if f.endswith('.csproj') else 0)\"",
            "timeout": 60,
            "statusMessage": "Rebuilding package lock file..."
          }
        ]
      }
    ]
  }
}
